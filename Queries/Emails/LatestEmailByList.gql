query LatestEmailByList(
    $slug: [String]
) {
  emailLists:latestEmailLists(where: {slug: $slug}) {
    nodes {
      emails(first: 1) {
        nodes {
            html
            link
          ...EmailParts
        }
      }
    }
  }
}
