query LatestEmailByList(
    $slug: [String]
) {
  emailLists(where: {slug: $slug}) {
    edges {
      node {
        emails(first: 1) {
          nodes {
            html
            link
            ...EmailParts
          }
        }
      }
    }
  }
}
